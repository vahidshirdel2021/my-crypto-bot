# V22 — Audit & Timeframe Update

- ثبت پایدار چرخه عمر هر معامله با Trade ID از سیگنال/پلن تا ورود، حفاظت و خروج.
- گزارش روزانه، ۷ روزه، ۳۰ روزه و کل سابقه.
- گزارش ممیزی آخرین معامله و خروجی JSON کامل معاملات/رویدادها.
- ثبت آمار اسکن، سیگنال، ورود، توقف و مهم‌ترین دلایل عدم ورود.
- نمایش چارت معامله هنگام باز شدن با Entry / SL / TP.
- دکمه ثابت پایین تلگرام فقط برای «☰ منوی اصلی».
- پیشنهاد تایم‌فریم بر اساس وضعیت تقریبی بازار؛ بدون تغییر خودکار تنظیمات.
- اصلاح import ماژول `re` برای جلوگیری از خطای `name 're' is not defined`.
- حفظ حالت PAPER/REAL، Gemini/OpenAI، لاگ تشخیصی و منوی اصلی موجود.

# V22.1 — Bugfix & Risk Patch (Claude review)

- رفع NameError در مسیر REAL که باعث توقف اضطراری کاذب بعد از هر معامله موفق می‌شد (`generation` تعریف‌نشده).
- کسر کارمزد تخمینی رفت‌وبرگشت (`TAKER_FEE_PCT`) از PnL نمایش‌داده‌شده در PAPER و در برآورد REAL؛ برای REAL با PnL واقعی صرافی، کارمزد فقط اطلاع‌رسانی می‌شود.
- تریلینگ‌استاپ پله‌ای (بر اساس R-multiple) به‌جای فقط یک‌بار انتقال به نقطه سربه‌سر.
- واچ‌لیست و تایم‌فریم پیش‌فرض محافظه‌کارانه‌تر برای کاربران/Session های تازه (۲۰ نماد نقدشونده، تایم‌فریم ۱۵ دقیقه) برای کاهش overtrading؛ تنظیمات کاربرهای موجود دست‌نخورده می‌ماند.
- هشدار امنیتی در لاگ هنگام خالی بودن `ALLOWED_CHAT_IDS`.
