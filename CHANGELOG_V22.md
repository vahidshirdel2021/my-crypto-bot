# V22 — Audit & Timeframe Update

- ثبت پایدار چرخه عمر هر معامله با Trade ID از سیگنال/پلن تا ورود، حفاظت و خروج.
- گزارش روزانه، ۷ روزه، ۳۰ روزه و کل سابقه.
- گزارش ممیزی آخرین معامله و خروجی JSON کامل معاملات/رویدادها.
- ثبت آمار اسکن، سیگنال، ورود، توقف و مهم‌ترین دلایل عدم ورود.
- نمایش چارت معامله هنگام باز شدن با Entry / SL / TP.
- دکمه ثابت پایین تلگرام فقط برای «☰ منوی اصلی».
- پیشنهاد تایم‌فریم بر اساس وضعیت تقریبی بازار؛ بدون تغییر خودکار تنظیمات.
- اصلاح import ماژول `re` برای جلوگیری از خطای `name 're' is not defined`.
- حفظ حالت PAPER/REAL، Gemini/OpenAI، لاگ تشخیصی و منوی اصلی موجود.

# V22.1 — Bugfix & Risk Patch (Claude review)

- رفع NameError در مسیر REAL که باعث توقف اضطراری کاذب بعد از هر معامله موفق می‌شد (`generation` تعریف‌نشده).
- کسر کارمزد تخمینی رفت‌وبرگشت (`TAKER_FEE_PCT`) از PnL نمایش‌داده‌شده در PAPER و در برآورد REAL؛ برای REAL با PnL واقعی صرافی، کارمزد فقط اطلاع‌رسانی می‌شود.
- تریلینگ‌استاپ پله‌ای (بر اساس R-multiple) به‌جای فقط یک‌بار انتقال به نقطه سربه‌سر.
- واچ‌لیست و تایم‌فریم پیش‌فرض محافظه‌کارانه‌تر برای کاربران/Session های تازه (۲۰ نماد نقدشونده، تایم‌فریم ۱۵ دقیقه) برای کاهش overtrading؛ تنظیمات کاربرهای موجود دست‌نخورده می‌ماند.
- هشدار امنیتی در لاگ هنگام خالی بودن `ALLOWED_CHAT_IDS`.

## V22 FINAL FIXED — امنیت و ممیزی کامل
- `ALLOWED_CHAT_IDS` اکنون اجباری است؛ در صورت خالی بودن، درخواست‌های Telegram مسدود می‌شوند.
- کش CoinEx با fingerprint کلیدها مدیریت می‌شود و در صورت تغییر credentials کلاینت قبلی استفاده نمی‌شود.
- Audit معامله شامل MFE/MAE، R واقعی، مدت معامله و اطلاعات excursion شد.
- گزارش عملکرد روزانه/هفتگی/ماهانه/کل، MFE/MAE و زمان متوسط معامله را نمایش می‌دهد.
- رویداد `signal_and_plan` از ابتدا quality score و planned R:R را ذخیره می‌کند.
- خطاهای fallback در `fmt`/`market_meta`/`normalize_amount`/`normalize_price` حداقل در سطح debug ثبت می‌شوند.
- منوی پایین چت همچنان حذف است و فقط Menu بومی تلگرام برای ورود به `/menu` استفاده می‌شود.


## V4 strategy integration
- Dynamic strategy updated to V4 candidate: strong bullish breakout only.
- Default strategy filters/config use min ADX 24, minimum score 82, no-short enabled, target cap 1.8R.
- Existing audit, AI, UI, public multi-user test access, and full watchlist are preserved.

## V24 — تشخیص خودکار جهت بازار (Auto Long/Short/No-Trade)
- تابع `refresh_market_regime` اضافه شد: جهت قطعی کل بازار را از هم‌راستایی BTC و ETH روی تایم‌فریم ۴ ساعته (قابل تنظیم با `MARKET_REGIME_TIMEFRAME`/`MARKET_REGIME_MIN_ADX`) تعیین می‌کند. فقط وقتی هر دو لیدر هم‌جهت باشند رژیم قطعی است؛ در غیر این صورت NEUTRAL و **هیچ معامله‌ای انجام نمی‌شود**.
- `strategy_dynamic` اکنون بر اساس این رژیم بین سیگنال‌های Long (شکست صعودی) و Short (شکست نزولی) سوییچ می‌کند؛ در NEUTRAL هیچ سیگنالی صادر نمی‌کند. این فقط استراتژی `dynamic` را تحت تأثیر قرار می‌دهد؛ سایر استراتژی‌ها (روندی/شکست/بازگشت/چندزمانه) بدون تغییرند.
- واچ‌لیست SHORT کاربر (`WINNING_SHORT_WATCHLISTS`) اضافه شد؛ در رژیم نزولی اسکن به‌جای واچ‌لیست LONG از این لیست استفاده می‌کند.
- نمادهای `LUNA` و `WAVES` (که در واچ‌لیست‌های ارسالی و حتی واچ‌لیست LONG قبلی بودند ولی در `ALL_SYMBOLS` نبودند) پس از تأیید وجودشان در CoinEx Futures اضافه شدند.
- `leader_correlation_guard` جهت‌دار شد: در جهت Long فقط جلوی ورود در سقوط تأییدشده لیدرها را می‌گیرد؛ در جهت Short، به‌طور متقارن جلوی ورود در جهش تأییدشده لیدرها را می‌گیرد (قبلاً این محافظ به‌طور یکسان هر دو جهت را در سقوط لیدرها مسدود می‌کرد که برای Short نادرست بود).
- گزارش «داشبورد بازار» رژیم قطعی فعلی برای استراتژی Dynamic را هم نمایش می‌دهد.
- ⚠️ برای فعال شدن ورودهای SHORT، فیلترهای `no_short_filter` و `no_buy_filter` باید از منوی فیلترها بررسی/تنظیم شوند (این فیلترها دستی هستند و به‌صورت خودکار تغییر داده نشدند).

## V24.1 — حذف نیاز به تنظیم دستی فیلتر جهت برای Dynamic
- `no_short_filter`/`no_buy_filter` اکنون فقط برای استراتژی‌های ثابت (روندی/شکست/بازگشت/چندزمانه) اعمال می‌شوند. برای استراتژی `dynamic`، این دو فیلتر در بررسی نهایی اجرای معامله نادیده گرفته می‌شوند چون جهت معامله قبلاً به‌طور قطعی توسط رژیم بازار (BTC+ETH) تعیین شده و این دو، در تناقض با آن قرار می‌گرفتند.
- منوی «⚙️ فیلترها» هنگام فعال بودن استراتژی Dynamic، یک یادداشت کوتاه نمایش می‌دهد که این دو فیلتر برایش نادیده گرفته می‌شوند.
- هیچ تغییری در رفتار سایر استراتژی‌ها یا در مقدار ذخیره‌شده‌ی این فیلترها اعمال نشد؛ اگر بعداً استراتژی را از Dynamic خارج کنید، تنظیمات قبلی همان‌طور که بودند دوباره اعمال می‌شوند.

## V24.2 — حذف هوش مصنوعی، ساده‌سازی منو، تأیید بستن پوزیشن، پارامتر per-timeframe
- **حذف کامل هوش مصنوعی:** تمام کد، کلید API، فیلد Session و دکمه‌های مربوط به Gemini/OpenAI (تحلیل هوشمند بازار/عملکرد، گفت‌وگو، تنظیمات AI) حذف شدند. AI هیچ‌وقت تصمیم معامله نمی‌گرفت، پس این حذف هیچ اثری روی منطق تریدینگ ندارد.
- **ساده‌سازی منو:** دکمه‌های «فیلترها»، «پارامترها» و «پیشنهاد تایم‌فریم» از منوی اصلی و منوی متنی حذف شدند. «بستن همه» طبق تصمیم کاربر نگه داشته شد. منطق فیلترها (`volume_filter`, `trailing_stop`, `candlestick_filter`) ثابت و روشن باقی می‌ماند؛ دیگر از منو قابل تغییر نیست.
- **تأیید بله/خیر برای هر نوع بستن پوزیشن:** دکمه‌ی «بستن {نماد}» در لیست پوزیشن‌ها، و «بستن همه خرید/فروش»، دیگر مستقیم نمی‌بندند و از یک صفحه‌ی تأیید عبور می‌کنند (مشابه چیزی که برای «بستن معامله» در صفحه‌ی مدیریت تک‌پوزیشن از قبل وجود داشت).
- **باگ قدیمی رفع شد:** دکمه‌ی تأیید «بستن همه» به دلیل یک شرط اشتباه در کد اصلاً هیچ‌وقت اجرا نمی‌شد (`/confirm_close_` عمومی جلوی `/confirm_close_all` را می‌گرفت). همین باگ برای `/confirm_close_longs`/`/confirm_close_shorts` هم که تازه اضافه شدند رفع شد.
- **پارامترهای ورود اختصاصی هر تایم‌فریم:** به‌جای یک آستانه‌ی ثابت و سخت‌گیرانه برای همه (ADX=24، حجم=1.15x، بدنه=0.55، امتیاز کیفیت=82، R:R=1.35)، حالا هر تایم‌فریم (5m/15m/1h/4h/multi) پیکربندی مستقل و متعادل‌تر خودش را دارد (`TIMEFRAME_STRATEGY_PRESETS` در `strategy.py`). این پیکربندی خودکار روی تغییر تایم‌فریم اعمال می‌شود؛ چون منوی تنظیم دستی پارامتر حذف شده، تایم‌فریم اکنون تنها منبع تعیین این پارامترهاست.
- تایم‌فریم ۱۵ دقیقه به‌طور کامل به لیست تایم‌فریم‌های پشتیبانی‌شده اضافه شد (دکمه در منو + واچ‌لیست LONG/SHORT مخصوص به خودش — چون واچ‌لیست تست‌شده‌ی جداگانه‌ای برای 15m نبود، فعلاً از واچ‌لیست 5m استفاده می‌کند؛ از منوی واچ‌لیست قابل تغییر است).
- آستانه‌ی `MARKET_REGIME_MIN_ADX` (تشخیص قطعی جهت کل بازار) از ۲۲ به ۱۸ کاهش یافت تا حالت NEUTRAL (بدون معامله) کمتر پیش بیاید.
- ⚠️ توجه: این تغییرات به‌صورت طراحی‌شده سخت‌گیری را کم می‌کنند تا فرکانس سیگنال بالاتر برود؛ توصیه می‌شود قبل از REAL، چند روز روی PAPER با تایم‌فریم‌های مختلف مقایسه شود.

## V24.3 — واچ‌لیست مشترک برای همه تایم‌فریم‌ها
- به‌جای ۵ واچ‌لیست جداگانه‌ی LONG (۷ تا ۱۶ نماد) و ۴ واچ‌لیست جداگانه‌ی SHORT، حالا فقط **دو** واچ‌لیست مشترک وجود دارد: یکی LONG (۳۸ نماد) و یکی SHORT (۵۸ نماد)، که هر دو برای همه‌ی تایم‌فریم‌ها (5m/15m/1h/4h/multi) یکسان استفاده می‌شوند.
- این دو لیست، اتحاد (union) همه‌ی نمادهایی هستند که قبلاً در هر کدام از لیست‌های اختصاصی هر تایم‌فریم/جهت بودند، به‌علاوه‌ی نمادهای نقدشونده‌ی اصلی؛ نه کل ۱۱۶ نماد `ALL_SYMBOLS` (که شامل آلت‌کوین‌های خیلی کم‌نقدشونده هم می‌شود و برای REAL می‌تواند اسپرد/لغزش بدی داشته باشد).
- جهت Long و Short عمداً همچنان **جدا** نگه داشته شدند (یکی نشدند) چون جهت یک محور معنادار جداست.
- از نظر عملکردی این تغییر ریسکی ندارد: تعداد درخواست هم‌زمان به CoinEx/KuCoin همچنان با سقف `MAX_ASYNC_REQUESTS=10` کنترل می‌شود؛ واچ‌لیست بزرگ‌تر فقط باعث می‌شود هر دور اسکن چند ثانیه بیشتر طول بکشد، نه خطا یا کندی در پاسخ‌دهی تلگرام.
