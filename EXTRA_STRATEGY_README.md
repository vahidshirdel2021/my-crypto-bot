# تغییرات این نسخه — استراتژی «اکسترا» + فیکس B3/S3/B7/S7 (بر اساس آدیت واقعی)

## بخش ۲: فیکس B3/S3/B7/S7 در `pdh_eq_pdl_engine.py` (جدید در این commit)

طبق آدیت واقعی (۷۹ معامله پیپر، ۵ آگوست تا ۳۱ اوت ۲۰۲۶، جدا روی ۵ و ۱۵
دقیقه)، این ۴ کد مشخصاً ضعیف عمل کرده بودند. به‌جای صرفاً «خاموش کردن»،
ریشه‌ی منطقی هرکدوم پیدا و اصلاح شد:

### B3/S3 (سوییپ مستقیم بدون عبور قبلی)
**مشکل واقعی در کد:** شرط «ری‌کلیم» (`long_reclaimed`/`short_reclaimed`)
فقط بررسی می‌کرد کلوز حداکثر `touch_tolerance_pct` (۰.۰۶٪) آن‌طرف سطح
نباشد — یعنی کلوزی که هنوز عملاً کمی زیر/بالای سطح بود هم به‌اشتباه
«ری‌کلیم موفق» حساب می‌شد. ترکیب این با حداقل بدنه‌ی کندل تاییدی خیلی
پایین (۰.۲۰) باعث می‌شد سیگنال‌های ضعیف زیادی رد بشه.

**فیکس:** برای همین دو کد (نه بقیه‌ی خانواده)، دو شرط اضافه اجباری شد:
- `direct_sweep_min_reclaim_margin_pct` (۰.۱۵٪): کلوز باید *واقعاً* فراتر
  از سطح باشد، نه فقط داخل تلورانس لمس.
- `direct_sweep_min_confirm_body_ratio` (۰.۳۵ به‌جای ۰.۲۰ عمومی): کندل
  تاییدی باید بدنه‌ی محکم‌تری داشته باشد.

این فیلتر روی هر دو طرف (B3 و S3) یکسان اعمال شد — چون داده نشون داد S3
(۱۶.۷٪ وین‌ریت) بدتر از B3 (۶۶.۷٪، هرچند نمونه کوچیک) بود، ولی نمونه S3
کافی نبود که مطمئن بشیم این عدم‌تقارن واقعاً از منطق کده یا صرفاً بایاس
جهت بازار توی همون بازه؛ برای همین یه فیلتر سخت‌گیرانه‌ی متقارن اعمال شد
تا از overfitting روی یک بازه‌ی خاص جلوگیری بشه.

### B7/S7 (ادامه مومنتوم بدون ری‌تست)
**مشکل واقعی در کد:** «پولبک کوتاه» فقط یعنی کندل قبلی قرمز/سبز بود — یک
کندل بازگشتی قوی و پرحجم هم این شرط رو رد می‌کرد، دقیقاً همون لحظه‌ای که
مومنتوم واقعاً داشت برمی‌گشت نه مکث می‌کرد. هیچ فیلتر اشباعی (RSI) هم
نبود، یعنی می‌تونست روی یه حرکت از قبل ته‌کشیده هم ورود بده.

**فیکس:**
- `momentum_pullback_max_body_ratio` (۰.۳۵): پولبک باید کم‌بدنه/مکث باشه.
- `momentum_rsi_exhaustion_high/low` (۷۸/۲۲): رد ورود اگه RSI از قبل در
  منطقه اشباع باشه.

**تصمیم محافظه‌کارانه:** با اینکه منطق فیکس شد، چون این دومین‌بار بود که
B7/S7 «فیکس و روشن» می‌شد و بازم داده منفی نشون داد، پیش‌فرض این‌بار به
**خاموش** برگردونده شد (هم در `ENGINE_DEFAULTS` و هم `TREND_MGMT_DEFAULTS`
در `bot.py`). با فیلترهای جدید، از منوی «مدیریت روند معاملات» می‌تونی
دستی روشنش کنی و داده‌ی تازه جمع کنی — ولی این‌بار توصیه می‌کنم قبل از
روشن کردن، حداقل چند روز صبر کنی و با دید باز («شاید بازم جواب نده») بهش
نگاه کنی، نه با فرض قبلی که حتماً درست شده.

**نکته مهم:** اگه سشن تلگرام از قبل ذخیره شده و `b7_s7_enabled=True` توش
ست شده، این پیش‌فرض جدید فقط روی سشن‌های *تازه* اعمال می‌شه. برای
اکانت‌های موجود، از منوی «مدیریت روند معاملات» به‌صورت دستی خاموشش کن تا
مطمئن بشی.

## بخش ۱: استراتژی «اکسترا» (Killzone + Judas Swing + MSS)

## چی اضافه شد

- **`extra_orb_engine.py`** (فایل کاملاً جدید): موتور مستقل با منطق متفاوت از
  PDH/EQ/PDL. به‌جای سطح ثابت روزانه، بر پایه‌ی *زمان سشن* (Killzone لندن
  ۰۷-۱۰ و نیویورک ۱۲-۱۵ به وقت UTC) کار می‌کند:
  1. Opening Range (رنج N دقیقه‌ی اول سشن) را ثبت می‌کند.
  2. منتظر Judas Swing (سوییپ یک طرف رنج) می‌ماند.
  3. با شکست ساختار (MSS) روی آخرین سوینگ مینور تایید می‌گیرد.
  4. اگر FVG هم همراهش باشد، امتیاز بونوس می‌گیرد.
  خروجی کد X1 (BUY) یا X2 (SELL) با entry/sl/tp عددی، دقیقاً هم‌شکل خروجی
  `evaluate_scenarios` در موتور اصلی.

- **`strategy.py`**: دو نقطه دیسپچ اضافه شد:
  - `get_signal_with_reason`: اگر `strategy_type=='extra'`، به‌جای موتور
    PDH/EQ/PDL از `evaluate_extra_scenarios` استفاده می‌کند.
  - `build_trade_plan`: **همین دیسپچ اینجا هم لازم بود** — این تابع قبلاً
    مستقل از `strategy_type` همیشه موتور قدیمی را دوباره صدا می‌زد؛ یعنی
    حتی اگر سیگنال اکسترا تشخیص داده می‌شد، پلن معامله هیچ‌وقت واقعاً باز
    نمی‌شد (چون موتور قدیمی روی دیتای dead-zone چیزی برنمی‌گرداند). این
    باگ در همین پچ برطرف شد.

- **`bot.py`**:
  - `WINNING_WATCHLISTS` فقط به `5min, 15min` محدود شد (۱ و ۴ ساعته موقتاً
    غیرفعال — برای فعال‌سازی دوباره، همون خط رو به تاپل قبلی برگردونید).
  - دو دستور جدید: `/set_tf_extra5m` و `/set_tf_extra15m` که هم‌زمان
    `timeframe` و `active_strategy='extra'` رو ست می‌کنن.

- **`ui.py`**: دکمه‌های «اکسترا ۵ دقیقه» و «اکسترا ۱۵ دقیقه» جایگزین دکمه‌های
  ۱ و ۴ ساعته در منوی انتخاب تایم‌فریم شدن.

## برای تست ۴ اکانت موازی

هر اکانت تلگرام یه سشن مستقل داره؛ کافیه توی هرکدوم از منوی «تایم‌فریم»:
- اکانت ۱ → «۵ دقیقه» (موتور فعلی، بعد از حذف B7/S3)
- اکانت ۲ → «۱۵ دقیقه» (موتور فعلی)
- اکانت ۳ → «اکسترا ۵ دقیقه» (موتور جدید)
- اکانت ۴ → «اکسترا ۱۵ دقیقه» (موتور جدید)

را انتخاب کنن. ریسک هر معامله (`risk_per_trade_pct`) رو عمداً **دست نزدم**
و همون پیش‌فرض فعلی مونده — طبق توصیه‌ای که دادم، فقط منطق جدید رو اضافه
کردم؛ تنظیم ریسک بمونه برای بعد از اینکه دیدیم خود منطق اکسترا اصلاً جواب
میده یا نه.

## محدودیت‌های شناخته‌شده (صادقانه)

1. **فقط مسیر زنده/پیپر (bot.py) آپدیت شد** — `backtest.py`، `batch_backtest.py`
   و `v3_backtest.py` هنوز فقط موتور قدیمی رو می‌شناسن. یعنی برای تست
   اکسترا فعلاً فقط از طریق ۴ اکانت تلگرام (پیپر زنده) می‌تونی داده جمع کنی،
   نه بک‌تست تاریخی سریع. اگه لازمه، در قدم بعد `v3_backtest.py` رو هم
   برای پشتیبانی از `strategy_type='extra'` آپدیت می‌کنم.
2. **پلکان TP سه‌مرحله‌ای برای اکسترا از فرمول ساده‌ی پیش‌رونده (۴۰٪/۷۰٪/۱۰۰٪
   مسیر تا هدف نهایی) استفاده می‌کنه**، نه از `tp_partial` (مرز Opening
   Range) که موتور جدید محاسبه می‌کنه — چون `_build_tp_ladder` فعلی برای
   ساختار PDH/EQ/PDL نوشته شده بود. اگه می‌خوای دقیقاً از ORB به‌عنوان پله
   اول استفاده بشه، باید یه مسیر جدا براش بنویسم.
3. **Killzone ساعت‌های ثابت UTC** (لندن ۷-۱۰، نیویورک ۱۲-۱۵) — استاندارد
   رایج ICT، ولی اگه ترجیح دیگه‌ای داری (مثلاً Asian session یا بازه‌های
   دقیق‌تر) به‌راحتی قابل تغییره (`EXTRA_ENGINE_DEFAULTS["killzones"]`).
4. این کد **تست واحد (unit test) روی داده واقعی نشده** — فقط از نظر
   نحوی (syntax) و منطقی بررسی شد. اولین چند سیگنالی که تولید می‌کنه رو
   حتماً دستی چک کن قبل از اینکه بهشون اعتماد کامل کنی.
