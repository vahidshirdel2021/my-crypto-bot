# یادداشت ادغام: منطق استراتژی B روی زیرساخت A

## چیکار شد
فقط فایل `strategy.py` با نسخه‌ی متعلق به `V2-01-patched-fixed-v2` جایگزین شد.
بقیه‌ی فایل‌ها (`bot.py`, `ui.py`, `backtest.py`, `v3_backtest.py`, تنظیمات، مستندات)
دقیقاً همون نسخه‌ی A هستن — هیچ خطی توشون دستکاری نشده.

## چرا این روش امن‌تر از ادغام دستی بود
همه‌ی توابعی که `bot.py`/`backtest.py`/`v3_backtest.py` نسخه‌ی A از `strategy.py`
وارد می‌کنن (import می‌کنن)، تو نسخه‌ی B هم با همون نام و همون ترتیب پارامترهای
اصلی وجود دارن؛ پارامترهای جدید همه در انتهای امضای تابع و با مقدار پیش‌فرض
اضافه شدن (نه وسط)، پس فراخوانی‌های موجود در A بدون تغییر کار می‌کنن.

## تست‌هایی که قبل از تحویل انجام شد
1. `ast.parse` روی هر دو فایل — بدون خطای syntax
2. import مستقیم `strategy`, `bot`... در واقع `backtest`, `v3_backtest` — هر سه موفق
3. فراخوانی واقعی `get_signal_with_reason` با داده‌ی OHLCV نمونه، دقیقاً با همون
   ترتیب آرگومان‌هایی که `bot.py` نسخه‌ی A استفاده می‌کنه — برای هم ۵ دقیقه هم
   ۱ ساعته، بدون کرش

## تفاوت‌های منطقی که الان روی A فعالن (که قبلاً نبودن)
- دروازه‌ی سخت‌گیرانه‌ی «اطمینان رژیم زیر ۰.۵۵ → رد فوری» حذف شد؛ الان فقط
  امتیاز رو تعدیل می‌کنه، بلاک نمی‌کنه
- برای ۵/۱۵ دقیقه، ۳ خانواده‌ی استراتژی امتحان می‌شه (`liquidity_sweep`,
  `trend_pullback`, `breakout_retest`) به‌جای ۱ تا
- برای ۱/۴ ساعته، استراتژی اختصاصی `strategy_htf_liquidity_reversal`
  (سطوح هفتگی/ماهانه‌ی واقعی) جایگزین سه‌تا استراتژی legacy شد
- سیستم «ستاپ فعال» (active setup recovery) اضافه شد
- فیلتر خلاف‌جهت بازار: قبلاً کاملاً بلاک می‌کرد، الان فقط امتیاز/RR لازم رو
  سخت‌تر می‌کنه (تاکس کیفیت، نه قفل ورود)
- ابزارهای دیباگ (`DEBUG_FORCE_ENTRY_5M`, دلایل خام رد شدن) هم روی این نسخه فعالن

## هنوز تست‌نشده
این ترکیب هیچ‌وقت رو یه سرور واقعی/PAPER اجرا نشده. قبل از REAL mode،
حتماً چند روز PAPER تست کن.
