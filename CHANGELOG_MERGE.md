# نسخه ادغام‌شده — بهترین ترکیب دو نسخه قبلی

این نسخه بر پایه‌ی `trading_bot_paper_v3_enhanced` ساخته شده (چون موتور استراتژی V2 و بک‌تستر
حرفه‌ای رو داشت) و فقط واچ‌لیست‌هاش با نسخه‌ی تخصصی‌تر جایگزین شده. هیچ تنظیم جدیدی به ربات
اضافه نشده — همه‌چیز با مقادیر پیش‌فرض خوب از قبل آماده است.

## چه چیزی تغییر کرد

**واچ‌لیست Long و Short از هم جدا شدند.**
قبلاً (در نسخه v3_enhanced) یک لیست ۱۱۴ نمادی مشترک هم برای Long و هم برای Short استفاده
می‌شد. این نسخه به جای آن از دو لیست تخصصی و کوچک‌تر که بر اساس بک‌تست جداگانه‌ی هر جهت
انتخاب شده بودند استفاده می‌کند:

- `LONG_WATCHLIST` (۲۹ نماد) — نمادهایی که در جهت صعودی عملکرد بهتری داشتند.
- `SHORT_WATCHLIST` (۵۱ نماد) — نمادهایی که در جهت نزولی عملکرد بهتری داشتند.

مکانیزم انتخاب لیست بر اساس Regime بازار (`scan_watchlist_for_timeframe`) از قبل در
v3_enhanced وجود داشت، فقط چون هر دو لیست یکی بودند اثری نداشت. حالا این مکانیزم واقعاً
دو دسته نماد متفاوت رو بسته به جهت بازار اسکن می‌کنه.

`ALL_SYMBOLS` (فقط برای fallback نمادهای فعال در حالت REAL هنگام نبود داده‌ی ذخیره‌شده) هم
به اتحاد دو لیست بالا (۵۸ نماد یکتا) به‌روزرسانی شد تا هیچ نمادی از قلم نیفتد.

## آپدیت دوم: استراتژی تایم ۵ و ۱۵ دقیقه ثابت شد

طبق درخواست، موتور V2 دیگر روی تایم‌فریم‌های ۵ و ۱۵ دقیقه بین چند خانواده استراتژی
(Trend/Breakout/Mean Reversion/Sweep) رقابت نمی‌گذارد. روی این دو تایم‌فریم **فقط**
استراتژی Liquidity Sweep اجرا می‌شود (همانی که از ابتدا مخصوص همین دو تایم‌فریم بود)،
ولی فیلترهای کیفیت V2 (حداقل امتیاز، حداقل RR، حداقل Edge Proxy، تشخیص Regime برای
جلوگیری از ورود خلاف روند شدید) همچنان روی همین یک استراتژی اعمال می‌شوند.

تایم‌فریم‌های ۱ ساعته، ۴ ساعته و مولتی دقیقاً مثل قبل انتخابی (چند-استراتژی) باقی مانده‌اند:
بسته به Regime تشخیص‌داده‌شده بین Trend/Breakout/Mean Reversion سوییچ می‌کنند.

## چیزی که تغییر نکرد

- موتور استراتژی V2 (تشخیص Regime، انتخاب Setup، فیلترهای score/RR/edge proxy) دقیقاً
  همان چیزیه که در v3_enhanced بود — سخت‌گیری فعلی (`min_setup_score=62`, `min_rr=1.3`,
  `min_edge_proxy=0.10`, سخت‌گیرتر در HIGH_VOL) دست‌نخورده باقی مانده، چون همین تنظیمات
  عامل کیفیت بالاتر سیگنال‌ها نسبت به نسخه‌ی اول بود.
- `PAPER_ONLY=true` به صورت پیش‌فرض همچنان فعاله — یعنی ربات با REAL شروع نمی‌شه مگر
  خودتون عمداً این متغیر محیطی رو عوض کنید.
- بقیه‌ی بخش‌ها (ریسک‌منیجمنت، دیتابیس، multi-user، بک‌تستر v3) دقیقاً همان نسخه‌ی
  v3_enhanced هستند.

## قبل از استفاده واقعی
مثل قبل: حتماً اول با PAPER تست کنید (پیشنهاد ۳۰ روزه در `README_PAPER_V3.md` هست)،
بعد با API Key و حجم خیلی کم وارد REAL بشید.
