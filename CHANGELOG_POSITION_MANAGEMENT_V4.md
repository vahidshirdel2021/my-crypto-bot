# V4 — Position Management Hardening

این نسخه فقط مدیریت پوزیشن و تمیزکاری امن کد را بهبود می‌دهد؛ منطق ورود، واچ‌لیست، تایم‌فریم معاملاتی و Long/Short تغییر نکرده است.

## تغییرات
- قیمت REAL فقط از همان CoinEx account/exchange پوزیشن خوانده می‌شود؛ دیگر برای مدیریت REAL به قیمت عمومی CoinEx تکیه نمی‌شود.
- داده بازار موردنیاز هر پوزیشن در هر چرخه یک‌بار گرفته می‌شود و به اجزای مدیریت پاس داده می‌شود تا درخواست تکراری کم شود.
- Hard SL/TP قبل از هر جابه‌جایی اختیاری استاپ بررسی می‌شود.
- Swing trailing روی تایم‌فریم اصلی معامله باقی می‌ماند تا ساختار اصلی حفظ شود.
- ضعف روند برای مدیریت سریع از تایم‌فریم پایین‌تر استفاده می‌کند:
  - 5m → 1m
  - 15m → 5m
  - 1h → 15m
  - 4h → 1h
- مدیریت سود مستقل از اندیکاتورها، برگشت 0.3R از MFE پس از حداقل 1R سود را برای حفظ سود بررسی می‌کند.
- Loss Cut هوشمند با افزایش ضرر حساس‌تر می‌شود و قبل از رسیدن به SL کامل می‌تواند خروج کند.
- بک‌تستر V3 برای مدیریت سریع از داده تایم‌فریم پایین‌تر استفاده می‌کند؛ در حالت آنلاین CoinEx این داده به‌صورت خودکار دریافت می‌شود.
- برای بک‌تست CSV، گزینه `--management-csv` برای ارائه داده دقیق تایم‌فریم مدیریت اضافه شده است.
- کد تکراری در محاسبه سطوح شبکه پاک شد.
- فایل‌های bytecode/cache از بسته نهایی حذف شدند.
