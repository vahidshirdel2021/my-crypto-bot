# راه‌اندازی نسخه Multi-User

## فایل‌ها

```text
bot.py
strategy.py
ui.py
requirements.txt
.env.example
```

`bot.py` فایل اجرایی اصلی است.

## نصب

```bash
pip install -r requirements.txt
python bot.py
```

## متغیرهای محیطی ضروری

```text
TELEGRAM_TOKEN=...
COINEX_ACCOUNTS_JSON=...
```

ساختار `COINEX_ACCOUNTS_JSON`:

```json
{
  "123456789": {
    "apiKey": "API_KEY_USER_A",
    "secret": "SECRET_USER_A"
  },
  "987654321": {
    "apiKey": "API_KEY_USER_B",
    "secret": "SECRET_USER_B"
  }
}
```

هر کاربر باید API Key و Secret حساب CoinEx خودش را داشته باشد. کلیدها را فقط با مجوزهای لازم برای Futures ایجاد کنید و در Git/Telegram/کد هاردکد نکنید.

## Multi-User

هر `chat_id` Session جدا دارد:

- تنظیمات استراتژی جدا
- فیلترها جدا
- واچ‌لیست جدا
- موجودی PAPER جدا
- پوزیشن‌های PAPER جدا
- وضعیت Daily Loss جدا
- API Client CoinEx جدا

در حالت REAL کاربر فقط به حسابی دسترسی دارد که همان `chat_id` در `COINEX_ACCOUNTS_JSON` برایش تعریف شده است.

## دیتابیس پایدار PostgreSQL / Neon (اختیاری)

اگر می‌خواهید دیتابیس مستقل از دیسک هاست باشد، کافی است یکی از متغیرهای زیر را با Connection String PostgreSQL تنظیم کنید:

```text
DATABASE_URL=postgresql://...
```

یا `NEON_DATABASE_URL` / `POSTGRES_URL`. اگر URL از نوع PostgreSQL باشد، ربات به‌صورت خودکار PostgreSQL را انتخاب می‌کند؛ اگر هیچ‌کدام تنظیم نباشند، رفتار قبلی SQLite حفظ می‌شود.

برای Neon معمولاً همان Connection String ارائه‌شده توسط Neon قابل استفاده است. اگر `sslmode` داخل URL نباشد، ربات برای اتصال امن آن را به `sslmode=require` تبدیل می‌کند.

**نکته:** تنظیم `DATABASE_URL` باعث می‌شود ربات از یک دیتابیس جدید/مستقل استفاده کند؛ داده‌های SQLite قبلی به‌صورت خودکار کپی نمی‌شوند. اگر لازم است تاریخچه و کاربران SQLite قبلی به Neon منتقل شوند، قبل از تغییر دیتابیس یک مهاجرت یک‌باره انجام دهید.

## نکته مهم درباره Restart

بعد از Restart، اسکن خودکار خاموش می‌ماند. برای حساب REAL ابتدا وضعیت پوزیشن‌های CoinEx بررسی و reconcile می‌شود و سپس کاربر می‌تواند اسکن را فعال کند.

## ⚠️ ماندگاری دیتابیس (خیلی مهم — قبل از استفاده واقعی حتماً بخوانید)

تمام پوزیشن‌ها، تاریخچه معاملات و آمار عملکرد در یک فایل SQLite ذخیره می‌شوند (مسیر پیش‌فرض:
`trader_bot.sqlite3`، کنار خودِ کد). **در اغلب پلتفرم‌های هاست (Render، Railway، Fly.io و مشابه)،
فایل‌سیستم پیش‌فرض غیردائمی (ephemeral) است** — یعنی با هر Deploy یا Restart، این فایل کاملاً از
بین می‌رود و همه‌چیز (پوزیشن‌های باز، تاریخچه، آمار) صفر می‌شود، حتی اگر خودِ کد هیچ باگی نداشته باشد.

برای جلوگیری از این اتفاق:
1. یک **Persistent Disk / Volume** جداگانه روی سرویس‌تان بسازید (در Render از بخش Disks، در
   Railway از بخش Volumes).
2. متغیر محیطی `BOT_DB_PATH` را به یک مسیر روی همان دیسک دائمی تنظیم کنید، مثلاً:
   ```text
   BOT_DB_PATH=/var/data/trader_bot.sqlite3
   ```
3. اگر `BOT_DB_PATH` تنظیم نشود، ربات هنگام بالا آمدن یک هشدار واضح در لاگ ثبت می‌کند و همچنین به
   `chat_id`های شناخته‌شده پیام می‌دهد که چند پوزیشن/تاریخچه بارگذاری شده — اگر عدد صفر بود در
   حالی‌که انتظار داده داشتید، یعنی همین مشکل رخ داده است.

روی VPS اختصاصی خودتان معمولاً این مشکل وجود ندارد (چون کل دیسک دائمی است)، فقط مطمئن شوید پروسه
با `systemd`/`pm2` مدیریت می‌شود تا با قطع اتصال SSH یا ریبوت از بین نرود.

## قبل از REAL

ابتدا با PAPER تست کنید. سپس API Key کاربر را اضافه کنید، موجودی و پوزیشن‌های CoinEx را بررسی کنید و با حجم بسیار کم اولین معامله REAL را انجام دهید.


### امنیت دسترسی
`ALLOWED_CHAT_IDS` در فاز تست اختیاری است. اگر مقداردهی شود، فقط شناسه‌های داخل آن مجاز خواهند بود؛ اگر خالی باشد، دسترسی عمومی برای تست فعال است. برای REAL استفاده از whitelist توصیه می‌شود.
