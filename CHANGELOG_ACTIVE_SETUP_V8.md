# V8 — Active Setup Revalidation + Live Position PnL

## اصلاحات این نسخه

### 1) Active Setup با Re-validation واقعی
- ستاپ قدیمی 5m/15m به‌تنهایی دیگر مجوز ورود نیست.
- پس از پیدا شدن ستاپ اخیر روی PDH/PDL، آخرین کندل بسته‌شده باید Pullback و Reclaim تازه روی همان سطح را تأیید کند.
- قیمت لحظه‌ای فقط برای جلوگیری از تعقیب قیمت (FOMO) استفاده می‌شود و به‌تنهایی سیگنال ورود ایجاد نمی‌کند.
- اگر قیمت بیش از محدوده مجاز از سطح فاصله گرفته باشد، ورود رد می‌شود.

### 2) سود/زیان لحظه‌ای واقعی در پیام پوزیشن‌ها
- پیام «پوزیشن‌ها» اکنون message_id خود را نگه می‌دارد.
- یک حلقه سبک هر 10 ثانیه قیمت فعلی را تازه می‌کند و همان پیام را ویرایش می‌کند.
- در PAPER و REAL قیمت فعلی جداگانه از منبع مناسب خوانده می‌شود.
- نمایش شامل قیمت فعلی، PnL لحظه‌ای، درصد بازده، R، TP و SL است.
- موتور سنگین مدیریت پوزیشن همچنان با فاصله اسکن اصلی اجرا می‌شود و حلقه Live PnL فقط نمایش را تازه می‌کند.

### 3) PAPER management
- قیمت فعلی برای PnL و تصمیمات لحظه‌ای از آخرین قیمت بازار استفاده می‌شود.
- High/Low کندل بسته‌شده همچنان برای بررسی برخورد TP/SL و excursion استفاده می‌شود تا رفتار بک‌تست تغییر نکند.

## تست
- `strategy.py`، `bot.py` و `ui.py` با `py_compile` بررسی شدند.
- Active Setup با سناریوی مصنوعی Pullback/Reclaim تست شد و سیگنال معتبر تولید کرد.
- سناریوی بدون Reclaim جدید تست شد و ورود رد شد.
