# V25 — رفع باگ گزارش تشخیصی + کاهش سخت‌گیری استراتژی Dynamic (Claude review)

## باگ رفع‌شده (نه تنظیمات)
- در `scan_loop`، وقتی رژیم بازار NEUTRAL بود، کد قبل از فراخوانی `scan_symbol` با `continue`
  کامل رد می‌شد. در نتیجه `_entry_diag_batch_update` هرگز صدا زده نمی‌شد و گزارش تشخیصی
  «چرا پوزیشنی باز نمی‌شود» هیچ‌وقت ارسال نمی‌شد — چون خودِ رژیم بازار اکثر اوقات NEUTRAL بود.
  حالا در این حالت یک نتیجه synthetic ساخته و به `_entry_diag_batch_update` پاس داده می‌شود؛
  گزارش طبق `NO_ENTRY_REPORT_SECONDS` (پیش‌فرض ۶۰۰ ثانیه = ۱۰ دقیقه) ارسال می‌شود.
- در `strategy_breakout`، آستانه ADX نهایی به‌صورت هاردکد روی `24.0` بود و از تنظیمات کاربر
  (`min_adx` در `strategy_config`) پیروی نمی‌کرد. حالا از `p["adx"]` استفاده می‌شود.

## کاهش سخت‌گیری (متوسط — قابل تنظیم مجدد)
- `MARKET_REGIME_MIN_ADX`: 22 → 18
- تشخیص رژیم: قبلاً هر دو لیدر (BTC و ETH) باید هم‌زمان و مستقلاً "قوی" می‌بودند. حالا اگر یک
  لیدر قوی صعودی/نزولی باشد و دیگری فقط خنثی (نه در جهت مخالف) باشد، رژیم همچنان قطعی اعلام
  می‌شود. تضاد مستقیم (یکی صعودی، دیگری نزولی) هنوز NEUTRAL می‌ماند.
- `min_trade_score`: 82 → 70
- `min_rr` / `min_target_r`: 1.35 → 1.25
- `strategy_breakout`: حجم شکست 1.15x → 1.08x، قدرت بدنه کندل 0.55 → 0.50

## نکته مهم
این تغییرات فراوانی سیگنال را افزایش می‌دهند اما لزوماً وین‌ریت را بالا نمی‌برند —
معمولاً trade-off است. پیشنهاد می‌شود حداقل ۱-۲ هفته در حالت PAPER تست شود و آمار
`min_trade_score` / `win rate` قبل و بعد مقایسه شود، قبل از فعال‌سازی روی REAL.
