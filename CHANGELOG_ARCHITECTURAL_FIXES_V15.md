# CHANGELOG — رفع اشکالات معماری (V15)

این نسخه ۶ درخواست اصلاحی را روی `bot.py`, `strategy.py`, `pdh_eq_pdl_engine.py`,
`backtest.py` و `v3_backtest.py` اعمال می‌کند. همه‌ی فایل‌ها با `py_compile`
بررسی شده‌اند و منطق‌های کلیدی با تست‌های واحد مصنوعی (synthetic) تأیید شده‌اند.

⚠️ **قبل از اجرای REAL حتماً روی PAPER تست کنید.** بخش بستن جزئی پوزیشن
(partial close) برای حالت REAL از الگوی سفارش بازار موجود در کد استفاده
می‌کند، اما امکان تست مستقیم آن روی صرافی زنده در این محیط وجود نداشت.

---

## ۱) رفع محاسبه سطوح PDH/EQ/PDL

**علت اصلی:** `compute_prev_day_levels`/`compute_prev_week_levels` در
`pdh_eq_pdl_engine.py` بدون بررسی اینکه دوره‌ی «قبلی» واقعاً کامل در داده
موجود است یا نه، سطح را محاسبه می‌کردند. چند نقطه در `bot.py` (`/analyze`،
چارت پس از ورود، `/view_chart_`) فقط ۱۶۰-۲۰۰ کندل می‌گرفتند — برای ۵ دقیقه
یعنی ۱۳-۱۶ ساعت، کمتر از یک روز کامل — که باعث می‌شد PDH/PDL از یک بازه‌ی
بریده محاسبه شود.

**تغییرات:**
- `compute_prev_day_levels`/`compute_prev_week_levels`: اضافه‌شدن بررسی
  کامل‌بودن دوره‌ی قبلی (حداقل ۸۵٪ تعداد کندل موردانتظار)؛ در غیر این‌صورت
  `None` برمی‌گردانند (به‌جای یک عدد نادرست/بریده).
- `min_klines_for_levels(timeframe)`: تابع جدید در `pdh_eq_pdl_engine.py` که
  حداقل کندل لازم برای هر تایم‌فریم را برمی‌گرداند (با حاشیه اطمینان ~۳
  دوره کامل).
- `bot.py`: `analyze()`, ارسال چارت بعد از باز شدن پوزیشن، و `/view_chart_`
  اکنون از `min_klines_for_levels(tf)` استفاده می‌کنند به‌جای اعداد ثابت
  ۱۶۰/۲۰۰.
- **رفع باگ چارت TAO/QNT:** `chart()` در `bot.py` دنبال ستون `_date` می‌گشت
  اما موتور ستون را `_period` نام‌گذاری کرده بود؛ شرط همیشه false می‌شد و
  چارت به‌جای کندل‌های «امروز»، صرفاً ۶۰ کندل خام آخر را نشان می‌داد.
  `_compute_prev_day_levels` در `strategy.py` اکنون یک ستون `_date` (برابر
  با `_period`) هم اضافه می‌کند.

---

## ۲) ممنوعیت سخت‌گیرانه ورود میان‌رنج

**علت اصلی:** سناریوهای **B6/S6** ذاتاً برای ورود «بدون لمس دقیق» PDL/PDH
طراحی شده بودند (ورود صرفاً بر اساس فاصله از EQ).

**تغییرات:**
- حذف کامل سناریوهای B6 و S6 از `pdh_eq_pdl_engine.py`.
- افزوده‌شدن یک دروازه‌ی سخت‌گیرانه‌ی «dead-zone» در ابتدای
  `evaluate_scenarios`: اگر در طول دوره‌ی جاری هیچ لمس/شکستی به PDH یا PDL
  رخ نداده باشد **و** کندل فعلی هم روی مرز نباشد، تابع فوراً `None` برمی‌گرداند
  — صرف‌نظر از فاصله تا EQ یا جهت قیمت. این دروازه جلوی هر سناریوی آینده‌ی
  مشابه را هم می‌گیرد.

---

## ۳) حذف کامل مدیریت هوشمند/خروج زودهنگام

**علت اصلی:** `_mfe_protection_exit_check` و `_weakness_exit_check` در
`bot.py` پوزیشن‌ها را بر اساس بازگشت از MFE یا امتیاز «ضعف روند» اندیکاتور،
پیش از رسیدن واقعی قیمت به TP/SL می‌بستند. `_apply_profit_protection`
(پلکان R قدیمی `PROFIT_LADDERS_R`) هم به‌طور مشابه SL را بر اساس فاصله‌ی R
جابه‌جا می‌کرد، مستقل از ساختار بازار.

**تغییرات:**
- حذف کامل توابع `_mfe_protection_exit_check`, `_weakness_exit_check`,
  `_apply_profit_protection`, `trailing_locked_r`, دیکشنری
  `PROFIT_LADDERS_R`, و `_position_management_timeframe` از `bot.py`.
- حذف ایمپورت `evaluate_trend_weakness` از `bot.py`؛ خود تابع هم از
  `strategy.py` حذف شد.
- حلقه‌ی `update_positions()` بازنویسی شد: اکنون تنها راه‌های خروج، پلکان
  TP (بند ۴) و برخورد واقعی به SL هستند.
- `backtest.py` و `v3_backtest.py` هم برای تطابق با معماری جدید به‌روزرسانی
  شدند (حذف شبیه‌سازی خروج زودهنگام).

---

## ۴) پلکان سه‌مرحله‌ای TP

**پیاده‌سازی:**
- `strategy.build_trade_plan` اکنون `tp1`/`tp2`/`tp3` و درصد هر پله
  (۵۰٪/۳۰٪/۲۰٪، قابل تنظیم با `tp_tier_pct`) را محاسبه می‌کند:
  - **حالت عادی** (اکثر سناریوها): tier1=EQ، tier2=مرز مقابل رنج (PDH/PDL خام)،
    tier3=اکستنشن فراتر از مرز مقابل.
  - **حالت بریک‌اند‌ریتست (B5/S5):** چون ورود پس از عبور از EQ و مرز رخ
    می‌دهد، این دو دیگر اهداف معتبر «جلوتر از ورود» نیستند؛ به‌جای آن‌ها سه
    پله‌ی پیش‌رونده (۴۰٪/۷۰٪/۱۰۰٪ مسیر تا هدف نهایی محاسبه‌شده توسط موتور)
    استفاده می‌شود تا هر سه پله واقعاً جلوتر از قیمت ورود باشند.
- **فیوز ایمنی جدید:** `build_trade_plan` اکنون معامله را رد می‌کند اگر هدف
  (`tp`) واقعاً جلوتر از قیمت ورود در جهت معامله نباشد (باگ پنهان قبلی: چک
  R:R با `abs()` این جهت را نادیده می‌گرفت).
- `bot.py`: فیلدهای `tp1/tp2/tp3`, درصدها, `tp1_done/tp2_done/breakeven_done`,
  `original_amount/original_margin` به دیکشنری معامله اضافه شد.
- تابع کاملاً جدید `_partial_close_position()`: بستن جزئی واقعی پوزیشن
  (PAPER و REAL)، با حسابداری صحیح کارمزد/فاندینگ per-tranche.
- `close_position()` بازنویسی شد تا کارمزد پلتفرم را فقط **یک‌بار** روی
  مجموع کل معامله (پله‌های جزئی + پای پایانی) بگیرد (چون `settle_platform_fee`
  بر اساس `trade_id` idempotent است).
- بلافاصله پس از برخورد پله اول (TP1/EQ)، SL کل باقی‌مانده به نقطه‌ی ورود
  (Break-even) منتقل می‌شود — هم در حافظه و هم (برای REAL) روی صرافی از
  طریق `move_stop_loss`.

⚠️ **محدودیت شناخته‌شده (REAL mode):** فقط SL و tp3 نهایی به‌صورت سفارش
حفاظتی native روی صرافی ثبت می‌شوند. tp1/tp2 روی صرافی سفارش جداگانه ندارند
و فقط توسط حلقه‌ی `update_positions()` (polling) با سفارش بازار reduce بسته
می‌شوند — یعنی اگر ربات آفلاین/کرش باشد، پله‌های ۱ و ۲ اجرا نمی‌شوند (SL و
tp3 هر شرایطی محافظت‌شده باقی می‌مانند). رفع این محدودیت (ثبت سفارش limit
جداگانه برای هر پله روی صرافی) در نوبت بعدی برنامه‌ریزی شده است.

---

## ۵) تریلینگ‌استاپ ساختاری بر اساس سوینگ با بافر

- `compute_swing_stop` در `strategy.py`: بافر نهایی اکنون
  `max(ATR × buffer_atr, قیمت‌سوینگ × buffer_wick_pct)` است — هم نوسان
  مطلق بازار (ATR) و هم نوسان نسبی دم کندل‌ها (wick %) را پوشش می‌دهد تا
  دم‌های عادی بازار باعث خروج زودهنگام نشوند.
- این تابع پس از Break-even هم فعال می‌ماند و طبق طراحی قبلی، فقط SL را
  بهبود می‌دهد، هرگز بدتر نمی‌کند.

---

## ۶) تشخیص سوئینگ و تنظیم جریمه فیک‌اوت

- `compute_swings` در `pdh_eq_pdl_engine.py`: دو فیلتر کیفیت اضافه شد —
  حداقل طول دم نسبت به ATR (`swing_min_wick_atr_ratio`, پیش‌فرض ۰.۱۵) و
  حداقل نسبت حجم به میانگین ۲۰ کندل (`swing_min_volume_ratio`, پیش‌فرض
  ۰.۶۰) — تا سوئینگ‌های نویزی/کم‌حجم مبنای سوییپ/ورود قرار نگیرند.
- `_dynamic_bonus_penalty`: جریمه‌ی ثابت ۱۵- برای «سابقه برخورد تکراری» اکنون
  به‌ازای هر سناریو با `penalty_scale_by_code` مقیاس می‌شود:
  B5/S5 → ۰.۳۵×, B3/S3 → ۰.۶۵×, B2/S2 → ۰.۶۰×. این ستاپ‌ها ذاتاً نیازمند
  چند برخورد متوالی به همان سطح هستند و جریمه‌ی ثابت به‌ناحق آن‌ها را زیر
  آستانه‌ی `min_score_to_trade` می‌انداخت.

---

## فایل‌های تغییریافته (V15)

| فایل | نوع تغییر |
|---|---|
| `pdh_eq_pdl_engine.py` | بازنویسی محاسبه سطوح، حذف B6/S6، دروازه dead-zone، فیلتر سوئینگ، مقیاس جریمه |
| `strategy.py` | پلکان TP، بافر سوینگ، حذف evaluate_trend_weakness، فیوز ایمنی هدف/ورود |
| `bot.py` | حذف مدیریت هوشمند، پلکان TP در اجرای معامله، `_partial_close_position`، رفع کمبود داده، رفع باگ چارت |
| `backtest.py` | همگام‌سازی کامل با معماری جدید (پلکان TP + تریلینگ سوینگ + حذف خروج هوشمند) |
| `v3_backtest.py` | حذف ایمپورت/شبیه‌سازی خروج هوشمند (سازگاری با strategy.py جدید) |

---

## به‌روزرسانی بعدی — تنظیم دقیق تایم‌فریم ۱۵ دقیقه

**درخواست:** بافر SL و آستانه حجم سوییپ مخصوص ۱۵ دقیقه.

**نکته فنی مهم:** `pdh_eq_pdl_engine.py` هیچ مکانیزم per-timeframe ندارد؛
`ENGINE_DEFAULTS` آن سراسری است و روی هر ۴ تایم‌فریم یکسان اعمال می‌شود. محل
واقعی و مؤثر برای override مخصوص یک تایم‌فریم، دیکشنری
`TIMEFRAME_STRATEGY_PRESETS` در `strategy.py` است — `bot.py` هنگام تغییر
تایم‌فریم جلسه، `s['strategy_config'] = get_timeframe_preset(tf)` را صدا
می‌زند و همین دیکشنری تا موتور اصلی (`evaluate_scenarios`) جریان پیدا
می‌کند. بنابراین تغییر مستقیم در `pdh_eq_pdl_engine.py` باعث می‌شد مقدار
روی **هر ۴ تایم‌فریم** اعمال شود، نه فقط ۱۵ دقیقه؛ برای همین override در
`TIMEFRAME_STRATEGY_PRESETS["15min"]` (در `strategy.py`) اعمال شد که تنها
مسیر مؤثر برای «فقط ۱۵ دقیقه» است. با تست مستقیم تأیید شد که مقدار override
تا سطح `_merged_cfg` موتور می‌رسد و روی سایر تایم‌فریم‌ها (۵ دقیقه، ۱ و ۴
ساعته) هیچ اثری ندارد.

**تغییرات (`strategy.py` → `TIMEFRAME_STRATEGY_PRESETS["15min"]`):**
- `sl_atr_buffer`: ۰.۳۵ (پیش‌فرض سراسری) → **۰.۴۵** فقط برای ۱۵ دقیقه.
- `swing_min_volume_ratio`: ۰.۶۰ (پیش‌فرض سراسری) → **۱.۲۰** فقط برای ۱۵ دقیقه
  (آستانه سخت‌گیرانه‌تر برای تایید حجم سوییپ نقدینگی).

**توجه:** سناریوی B5/S5 (بریک‌اند‌ریتست) از پارامتر جداگانه‌ای به نام
`sl_atr_buffer_tight` استفاده می‌کند، نه `sl_atr_buffer`؛ این پارامتر تغییر
نکرده و همچنان سراسری است. اگر تنظیم دقیق `sl_atr_buffer_tight` مخصوص ۱۵
دقیقه هم مدنظر است، باید جداگانه درخواست شود.

---

## توصیه نهایی

قبل از فعال‌سازی حالت REAL، حتماً یک دوره‌ی کامل PAPER اجرا کنید و به‌خصوص
پیام‌های `TP1`/`TP2`/بستن جزئی و انتقال SL به Break-even را در لاگ/تلگرام
بررسی کنید.
